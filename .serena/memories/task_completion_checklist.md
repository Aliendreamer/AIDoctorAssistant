# Task Completion Checklist

When a coding task is complete:
1. `dotnet build` — ensure no build errors
2. `dotnet test` — all tests pass
3. `dotnet format` — code is formatted
4. Review with Serena LSP diagnostics if available
5. Update OpenSpec change tasks to mark completed
6. Commit via git
